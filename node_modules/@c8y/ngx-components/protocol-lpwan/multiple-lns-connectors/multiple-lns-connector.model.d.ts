export declare enum ConnectionType {
    ACTILITY = 0,
    SIGFOX = 1
}
export interface Connection {
    name: string;
    baseUrl: string;
    username: string;
    password: string;
    description: string;
}
export interface SigfoxConnection extends Connection {
    parentGroupId: string;
}
export interface ActilityConnection extends Connection {
    profileId: string;
    routeApplicationServerId?: string;
    routeApplicationServerKey?: string;
    adminApiVersion: string;
    coreApiVersion: string;
}
export declare function isSigfoxConnection(connection: SigfoxConnection | ActilityConnection): connection is SigfoxConnection;
export declare function isActilityConnection(connection: SigfoxConnection | ActilityConnection): connection is ActilityConnection;
export declare type State = 'loadingConnection' | 'loadingError' | 'connectionAvailable' | 'connectionNotAvailable' | 'addConnections' | 'savedSuccessfully' | 'updateConnection';
